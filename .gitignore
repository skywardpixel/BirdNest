# Extension signing key — determines the extension ID. Never commit.
extension/key.pem
*.pem

# Rendered by `birdnest install-host` from manifest.template.json, with this
# machine's public key pinned as `key`. Generated, and per-clone. Never commit.
extension/manifest.json

__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.pytest_cache/

# Throwaway clips used for manual clipboard testing
testdata/
